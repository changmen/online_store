<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.onlinestore.mapper.OrderItemMapper">
    <resultMap id="orderItemResultMap" type="com.example.onlinestore.entity.OrderItemEntity">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="productImage" column="product_image"/>
        <result property="price" column="price"/>
        <result property="quantity" column="quantity"/>
        <result property="subtotal" column="subtotal"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="orderItemColumns">
        id, order_id, product_id, product_name, product_image, price, quantity,
        subtotal, created_at, updated_at
    </sql>

    <select id="findByOrderId" resultMap="orderItemResultMap">
        SELECT <include refid="orderItemColumns"/>
        FROM order_item WHERE order_id = #{orderId}
    </select>

    <insert id="insert" parameterType="com.example.onlinestore.entity.OrderItemEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (
            order_id, product_id, product_name, product_image, price, quantity,
            subtotal, created_at, updated_at
        ) VALUES (
            #{orderId}, #{productId}, #{productName}, #{productImage}, #{price}, #{quantity},
            #{subtotal}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO order_item (
            order_id, product_id, product_name, product_image, price, quantity,
            subtotal, created_at, updated_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage},
                #{item.price}, #{item.quantity}, #{item.subtotal}, #{item.createdAt},
                #{item.updatedAt}
            )
        </foreach>
    </insert>
</mapper> 